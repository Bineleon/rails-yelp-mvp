<h1>Our restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
 <li><%= restaurant.name %></li>
 <li><%= link_to "See details", restaurant_path(restaurant), class: "btn btn-light"%></li>
  <% end %>
</ul>


<%= link_to "Add a restaurant", new_restaurant_path(@restaurant) %>
